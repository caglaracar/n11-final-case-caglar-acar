# ─── n11-client-admin (Vite) — local dev ──────────────────────────
# Kullanım:
#   cp .env.example .env
# Vite VITE_* prefix'i olan env'leri client'a expose eder.

VITE_API_URL=http://localhost:8080
VITE_APP_ENV=local
